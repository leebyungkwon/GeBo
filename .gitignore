# 노드 모듈
node_modules/

# 임시/출력 파일
out.json
check_db_out.txt

# 환경변수 파일
.env
.env.local
.env.*.local

# IDE
.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# 빌드 산출물 (각 하위 프로젝트에서 관리)
# bo/.next, bo-api/target 은 하위 .gitignore에서 처리

# 플러그인 내부 resolved 파일 (바이너리/캐시성)
.plugin/**/*.resolved
.plugin/**/*.resolved.*
.plugin/**/*.metadata.json
