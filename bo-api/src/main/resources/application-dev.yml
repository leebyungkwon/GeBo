# 개발 서버 환경 설정 (Supabase) — 민감 정보는 환경변수로 주입
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

# CORS - 배포된 FE 주소 (Vercel) — 환경변수로 주입
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
